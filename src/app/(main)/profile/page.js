import React from "react";
import Profile from "./Profile";

export const metadata = {
  title: "Profile",
  description: "Profile page",
};
const page = () => {
  return <Profile />;
};

export default page;
